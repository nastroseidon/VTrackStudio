---
**COPY THIS ENTIRE SECTION AND PASTE INTO NEXT TASK'S CHAT**

---

# HANDOFF: M3.5 → M3.6

You are starting work on **VTrackStudio CourseForge** — golf course reconstruction from open geospatial data (OSM geometry + Copernicus elevation + ESA WorldCover land cover → a neutral `CoursePackage`). This is a coordinated multi-session project.

**Read this whole file before running anything.** Four blockers in §1 must be resolved with the user first; they are not yours to decide unilaterally.

---

## 1. BLOCKERS — resolve these before writing any code or docs

These were found by verification on 2026-07-24 and are unresolved. Do not skip.

### 1a. Two sessions share one working directory — this destroys work

`H:\Claude\VTrackStudio` is being used by more than one agent session at once. On 2026-07-24 a session ran `git checkout main` plus a `reset` mid-flight in this directory while another session had uncommitted M3.5 work on `feature/splat-api-ui-wiring`. That work was wiped. Reflog evidence:

```
4caccd8 HEAD@{2}: checkout: moving from feature/splat-api-ui-wiring to main
4caccd8 HEAD@{3}: reset: moving to HEAD~1
```

M3.5 was consequently **built twice, in parallel, by two sessions**. This is the second occurrence — the same thing happened at M3.4 (see `AUTOMAT_PORT_HANDOFF.md` §5, PR #19 vs PR #21).

File-ownership rules do not fix this. Concurrent `git checkout` in a shared tree destroys uncommitted work regardless of who owns which file. **Fix the isolation before starting M3.6:**

- Give each session its own `git worktree` (the Codex session already has one at `H:\Claude\VTrackStudio-codex`), **or**
- Run only one session against this directory at a time.

Ask the user which. Do not begin M3.6 until this is settled.

### 1b. `main` has 4 unpushed commits made directly to it

Local `main` is 4 ahead of `origin/main`. `AGENTS.md` and `CODEX_BRIEF.md` both say never commit to `main`.

```
26c4b55 refactor: make handoffs self-contained and copy-paste ready
1c78575 Add handoffs/ directory with M3.5-to-M3.6 prompt stored in repo
7c54efc docs: update README.md with coordination file pointers and new-task quick start
d9ce4ed Add coordination infrastructure: CODEX_BRIEF, HANDOFF, TASK_LEDGER, REFOCUS
```

All four are docs/coordination only — no source changes — so the damage is low. Ask the user whether to move them onto a branch and open a PR, or accept them in place. Do not push `main` without approval.

### 1c. M3.6's scope is defined two contradictory ways

- `courseforge/docs/PHASE3_LANDCOVER_SPLAT_DESIGN.md` §7 (signed off 2026-07-23): **M3.6 = canopy/trees** — tree-cover class + Meta/WRI canopy → foliage instances, with its own `LIVE` gate and schema addition. M3.7 = merge to `main`.
- The previous version of this handoff: **M3.6 = docs only** — archive Phase 3, write a Phase 4 roadmap, write a completion summary.

These are different milestones with different risk profiles (one is a gated live provider + schema change; the other is prose). **Ask the user which M3.6 is.** The signed-off design document is the stronger authority, but the user may have re-planned deliberately.

### 1d. M3.5 shipped with zero new tests

`feature/surfaces-api-ui` passes `verify:fast` at **118 tests** — identical to `main`'s 118. No test was added for the new API route, the bundle surface-layer path, or the `surfaces` field on `CoursePackage`. Every prior milestone in Phases 1–3 added tests.

Flag this to the user. Recommended: add coverage before merging M3.5, or file it as an explicit follow-up.

---

## 2. Verified current state (checked 2026-07-24, not inherited)

### `main` — `4caccd8` on origin, `26c4b55` locally
`verify:fast` **PASS**: lint clean, typecheck clean, **118 tests / 15 test files**, `next build` clean.

> Note: "15/15" in earlier notes meant 15 **test files**, not 15 tests. The real count is 118.

### Milestones

| Milestone | Phase | State | Evidence |
|---|---|---|---|
| M1.x | Phase 1 — OSM geometry | ✅ merged | PR #6 → `3dae504` |
| M2.1–M2.5b | Phase 2 — Copernicus GLO-30 | ✅ merged | PR #7 → `7356a10` |
| M2.6 | Phase 2 — multi-tile mosaic | ✅ merged | PR #9 → `0cf1bfa` |
| M3.1 | Splat schema + encoder | ✅ merged | PR #10 → `337e32a` |
| M3.2 | Polygon rasteriser | ✅ merged | PR #13 → `40ab2d3` |
| M3.3 | WorldCover live provider | ✅ merged | PR #21 → `4caccd8` |
| M3.4 | Compositor | ✅ merged | PR #19 → `778dc24` |
| **M3.5** | **API + UI + bundle** | ⚠️ **PR #23 OPEN, not merged** | `b77862e` on `feature/surfaces-api-ui`, mergeable |

Merged-PR numbers above are read from `git log`. Note: the previous handoff's **"#23" for M3.5 was correct** — that PR exists and is open. What was wrong was marking M3.5 "✅ Complete"; an open PR is not a merged one. Its "#13, #15" for Phase 2 were wrong (#13 is M3.2).

### M3.5 as actually built — `b77862e`

Verified on the branch: `verify:fast` passes, 118 tests, `/api/surfaces/generate` registers in the build output.

- `app/api/surfaces/generate/route.ts` — POST; live keyless WorldCover fetch, OSM-over-landcover precedence, returns splat descriptor + per-layer base64 bytes
- `app/page.tsx` — `handleGenerateSurfaceLayers`, `surfaceLayersB64Ref` client cache
- `app/api/course-package/bundle/route.ts` — surface layers into the ZIP; **has a server-side regeneration fallback** when the client cache is cold (uses `providerCourseId`), returns 409 when it cannot regenerate
- `components/ProjectStatusRail.tsx` — "Generate Surface Layers (WorldCover)" button + surfaces summary
- `lib/course-package/build-course-package.ts` — `surfaces` passed into the package
- `packages/course-schema/src/courseProject.ts` — `surfaces?: CourseSplatMap` on `CourseProject`

**A duplicate M3.5 branch `feature/splat-api-ui-wiring` exists and is empty** (its work was wiped per §1a). Delete it; `b77862e` is canonical.

### End-to-end pipeline working
OSM geometry → Copernicus GLO-30 elevation → ESA WorldCover surfaces → `CoursePackage` ZIP. All sources open and keyless. Hand-rolled deterministic PNG encoding. 16-bit heightmap + 8-bit splat layers on a shared grid. Hard masks only — feathering is the engine's job (Phase 3 design §10.3).

---

## 3. Governance

**Council scoring** — Correctness 25%, Safety 20%, Testability 15%, Rollback 10%, Fit 10%, Scope 10%, Maintainability 5%, Simplicity 5%.

- **≥85** → auto-proceed: commit, push, open PR without asking
- **<85** → present to council first
- **Merge to `main`** → always requires explicit user approval, no exceptions
- Never ask the user anything without a score and a recommendation attached

**Live external providers** and **schema changes** are separate approval gates. If M3.6 turns out to be canopy/trees (§1c), it needs both.

**No paid APIs or paid asset licensing, ever.** Free + attribution-only sources only (CC-BY, CC0, ODbL, public domain).

**Anti-patterns:** no features outside the current milestone; no refactors or invented abstractions without explicit need; no "future flexibility" config. Read `REFOCUS.md` before any council submission.

---

## 4. Coordination files (repo root)

| File | Purpose |
|---|---|
| `README.md` | Entry point |
| `CODEX_BRIEF.md` | Structure, governance, file ownership, workflow |
| `AUTOMAT_PORT_HANDOFF.md` | Long-running cross-session project record — **the deepest history; read it** |
| `HANDOFF.md` | Current-state summary |
| `TASK_LEDGER.md` | Check-in/check-out registry — mark IN_PROGRESS when you start |
| `REFOCUS.md` | Anti-scope-creep decision tree |
| `handoffs/` | Handoff prompt history |

`AUTOMAT_PORT_HANDOFF.md` is the authoritative record and is more detailed than `HANDOFF.md`. Where they disagree, trust `AUTOMAT_PORT_HANDOFF.md` and the design docs, then verify against `git log`.

**Known-stale content:** treat milestone/PR claims in these files as unverified. Several were wrong as of 2026-07-24 (see §2). Verify against `git log` before relying on any of them.

---

## 5. Environment

```
User Email:   n9smith@gmail.com
Node:         24.18.0 (portable) at H:\Claude\tools\node-v24.18.0-win-x64  — NOT on PATH
Memory:       C:\Users\n9smi\.claude\projects\H--Claude\memory\
Date:         2026-07-24
Repo:         https://github.com/nastroseidon/VTrackStudio
Working dir:  H:\Claude\VTrackStudio     (see §1a before using)
```

Node must be added to PATH each session. Git Bash:

```bash
export PATH="/h/Claude/tools/node-v24.18.0-win-x64:$PATH"
```

All npm commands run from `courseforge/apps/web/` — there is no root package. Git Bash has no node/npm without the export above; PowerShell is Windows PowerShell 5.1.

### Verified tooling (re-checked 2026-07-24 — earlier notes were wrong)

| Tool | State |
|---|---|
| `gh` | **Installed and authenticated** — 2.96.0 via WinGet, on PATH, account `nastroseidon`, scopes `gist repo read:org workflow`. Use it for PRs. |
| `git` | 2.54.0. GCM holds a write-scoped credential; `git push` works. |
| Node | 24.18.0 portable, **not** on PATH — export it (above). Works. |
| Docker | CLI 29.6.1 installed and on PATH, but the **daemon is not reachable** (`dockerDesktopLinuxEngine` pipe missing). Don't rely on it. |
| PowerShell | Windows PowerShell 5.1 — no `&&`/`\|\|`, no ternary. Bash tool is Git Bash. |

> `AUTOMAT_PORT_HANDOFF.md` §6 claims `gh` is not installed. **That is false** and cost a round-trip. That file lives at `H:\Claude\`, *outside* the repo and outside version control — it cannot be fixed by a PR. Verify tooling with `which <tool>` rather than trusting either document.

**Gotcha:** `.next/types` can go stale and fail `typecheck` with phantom "cannot find module" errors on newly added routes. `rm -rf .next` before `verify:fast` when you add a route.

### Skills
`/review` (quality pass), `/simplify` (clarity edit), `/dataviz` (diagrams), `/security-review`.

> There is **no `/remote-control` skill.** The previous handoff opened with `/remote-control on`; that command does not exist and will do nothing. Autonomy comes from the council ≥85 rule in §3, not from a slash command. Do not paste it.

---

## 6. First steps

### Step 1 — Resolve the §1 blockers with the user
Isolation (1a), unpushed `main` (1b), M3.6 scope (1c), missing M3.5 tests (1d). Present each with a council score and a recommendation. **Nothing below is safe to start until 1a and 1c are answered.**

### Step 2 — Confirm the baseline yourself
```bash
cd /h/Claude/VTrackStudio
git fetch origin
git status
git log --oneline -5
```
```bash
export PATH="/h/Claude/tools/node-v24.18.0-win-x64:$PATH"
cd courseforge/apps/web && npm ci && npm run verify:fast
```
Expect **118 tests passing**, lint/typecheck/build clean. If not, stop and report — do not build on a red baseline.

### Step 3 — Read, in order
1. `AUTOMAT_PORT_HANDOFF.md` — full history
2. `courseforge/docs/PHASE3_LANDCOVER_SPLAT_DESIGN.md` — §7 milestones, §10 sign-off amendments
3. `CODEX_BRIEF.md` — file boundaries
4. `REFOCUS.md` — scope discipline

### Step 4 — Check out your task
```bash
git checkout main
git pull origin main
git checkout -b <feature-or-docs-branch-for-the-agreed-M3.6>
```
Then update `TASK_LEDGER.md` to `IN_PROGRESS` with your task id and path, and commit that first.

### Step 5 — Do the work
Follow the M3.6 definition agreed in Step 1. Add tests for anything executable. Run `verify:fast` per milestone; `npm run verify` (adds Playwright) for anything touching the browser.

### Step 6 — Hand off
Update `TASK_LEDGER.md` to COMPLETE, refresh `AUTOMAT_PORT_HANDOFF.md`, and write the next `handoffs/*.md`. **Verify every claim you put in it against `git log` and a real test run** — the errors corrected in this document existed because that was not done.

---

## 7. Coordination rules that have already been violated once

- Canonical class→layer map lives in `composite-surfaces.ts`. Splat entry point is `generateCourseSplatMap` in `generate-splat.ts`. **Do not recreate duplicates** — this caused the M3.4 collision.
- `courseforge/apps/web/lib/**`, `components/**`, `app/**`, `packages/course-schema/**`, `tests/**` are reserved for the main Phase-3 session. The Codex session owns `.github/**`, `unreal/**`, `courseforge/docs/PHASE6_*.md`.
- Re-confirm boundaries with any parallel session **before** starting, not after.
- Never force-push. Never rewrite history. Never merge to `main`.

---

**Start with §1. Do not skip to §6.**
